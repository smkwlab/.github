{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>smkwlab/.github:latex#v1"],
  "description": [
    "Document templates. The configuration currently matches the latex preset exactly, but the constraint behind it is different and the seam is worth keeping: a template's contents are copied verbatim into every student repository generated from it, and those repositories have no Renovate. Anything pinned in a template is frozen there for good, whatever the infrastructure repositories decide to do.",
    "The shared-workflow reference is kept on the moving v1 tag by the latex preset, which is why no rule is repeated here. Should that preset ever pin it again, this preset is where the templates opt back out."
  ]
}
